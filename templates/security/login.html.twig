{% block stylesheets %}
    {{ encore_entry_link_tags('login') }}
{% endblock %}

{% block body %}
<div class="container">
    <div class="login-form">
        <a href="{{ path('app_login') }}">SmileCare</a>
        <div class="login">
            <form method="post">
                {% if error %}
                    <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}

                {# {% if app.user %}
                    <div class="mb-3">
                        You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
                    </div>
                {% endif %} #}

                <h1 class="h3 mb-3 font-weight-normal">Connexion</h1>

                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" placeholder="Email" required autofocus>
                <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" placeholder="Mot de Passe" required>
                <a href="#" class="forgot-password-link">Mot de passe oublié ?</a>
                <input type="hidden" name="_csrf_token"
                    value="{{ csrf_token('authenticate') }}"
                >
                <span class="login-button">
                    <button class="btn btn-lg btn-primary" type="submit">Se connecter</button>
                </span>
                <span class="register-button">
                    <button><a href="{{ path ('app_register') }}">S'enregistrer<a></button>
                </span>
            </form>
        </div>
    </div>

    <div class="intro">
        <div class="intro-text">
            <h2>Bienvenue !</h2>
            <h3>Que dis-tu de suivre ton hygiène bucco-dentaire?</h3>
            <p>Cette application te permettra de suivre tes progrès au fil du temps et 
            <br>avoir des conseils et astuces personnalisés pour prendre soin 
            <br>de ta santé bucco-dentaire ! 
            <br><br>Connecte-toi ou crée ton compte dès maintenant et découvre 
            <br>tous les avantages de notre application pour un sourire radieux ! </p>
        </div>
        <div class="help">
            <a href="#">Besoin d'aide ?</a>
        </div>
    </div>

</div>
{% endblock %}
